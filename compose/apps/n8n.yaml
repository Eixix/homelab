services:
  n8n:
    container_name: n8n
    image: docker.n8n.io/n8nio/n8n:latest
    restart: unless-stopped
    user: ${N8N_UID}:${N8N_GID}
    environment:
      TZ: ${TZ}
      HOME: /home/node/.n8n
      N8N_USER_FOLDER: /home/node
      XDG_CACHE_HOME: /home/node/.n8n/.cache
      GENERIC_TIMEZONE: ${TZ}
      N8N_HOST: ${N8N_HOST}
      N8N_PROTOCOL: https
      N8N_EDITOR_BASE_URL: https://${N8N_HOST}
      WEBHOOK_URL: https://${N8N_HOST}
      N8N_PROXY_HOPS: 1
    volumes:
      - ../../data/n8n:/home/node/.n8n
      - ../../data/n8n/.cache:/home/node/.cache
    networks:
      - internal_network
    labels:
      - traefik.enable=true
      - traefik.http.routers.n8n.rule=Host(`${N8N_HOST}`)
      - traefik.http.routers.n8n.entrypoints=websecure
      - traefik.http.routers.n8n.tls=true
      - traefik.http.routers.n8n.tls.certresolver=internalresolver
      - traefik.http.routers.n8n.middlewares=internal-app@file
      - traefik.http.services.n8n.loadbalancer.server.port=5678
      - traefik.docker.network=internal_network
